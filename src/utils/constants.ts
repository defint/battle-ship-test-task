import { ShipModel } from './types';

export const AREA_SIZE = 10;
export const SIMULATION_DELAY_MS = 100;
export const shipModelsList = [
  ShipModel.CURVED,
  ShipModel.LINE,
  ShipModel.LINE,
  ShipModel.LINE,
  ShipModel.LINE,
  ShipModel.LINE,
  ShipModel.LINE,
  ShipModel.LINE,
  ShipModel.LINE,
  ShipModel.LINE,
  ShipModel.SINGLE,
  ShipModel.SINGLE,
];
